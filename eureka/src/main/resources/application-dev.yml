server:
  port: 6080
spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false #注册到Eureka的注册中心
    fetch-registry: false #获取注册实例列表
    service-url:
      defaultZone: http://localhost:6080/eureka/ #配置注册中心地址
logging:
  config: classpath:logback.xml

  
  
  
